// Generated by CoffeeScript 1.7.1
(function() {
  var REGISTRY, TRM, alert, badge, debug, echo, help, info, log, rainbow, rpr, urge, warn, whisper;

  TRM = require('coffeenode-trm');

  rpr = TRM.rpr.bind(TRM);

  badge = 'TIMETABLE/timetable-from-gtfs-data';

  log = TRM.get_logger('plain', badge);

  info = TRM.get_logger('info', badge);

  whisper = TRM.get_logger('whisper', badge);

  alert = TRM.get_logger('alert', badge);

  debug = TRM.get_logger('debug', badge);

  warn = TRM.get_logger('warn', badge);

  help = TRM.get_logger('help', badge);

  urge = TRM.get_logger('urge', badge);

  echo = TRM.echo.bind(TRM);

  rainbow = TRM.rainbow.bind(TRM);

  REGISTRY = require('./REGISTRY');

  this.$add_n4j_system_properties = function(isa, label) {
    return this.$((function(_this) {
      return function(record, handler) {
        record['~isa'] = isa;
        record['~label'] = label;
        return handler(null, record);
      };
    })(this));
  };

  this.timetable_from_gtfs_data = function(gtfs_registry, handler) {};

  if (module.parent == null) {
    this.main(function(error, registry) {
      if (error != null) {
        throw error;
      }
      return info(registry);
    });
  }

}).call(this);
